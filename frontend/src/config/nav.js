// Page ids are internal (Dashboard switches on them, Xibo's vocabulary shows
// through in places). Labels are what the customer reads, and they have to be
// identical in the sidebar and the topbar — the heading saying "Screens" while
// the nav says "Display" reads as two different pages.
export const PAGE_LABELS = {
  display: "Screens",
  layout: "Layouts",
  schedule: "Schedule",
  library: "Library",
  playlist: "Playlists",
  dataset: "Data sources",
};
